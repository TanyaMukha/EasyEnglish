@namespace EasyEnglish.Theme.Components

@* Максимально спрощений ThemeProvider *@
@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public string LoadingMessage { get; set; } = "Loading Theme Demo...";
}